How to Reload
=============

| When the page needs to be refreshed (eg. page has timed out, a record has recently been added and has not yet appeared), you should reload the page.
| You should navigate to the page you wish to reload using the :icon:`bars` in the ribbon.

#. Simply click the :icon:`refresh` in the right hand corner of the page
